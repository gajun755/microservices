spring:
  cloud:
    config:
      uri: http://localhost:8085
        
  application:
    name: account_service
  profiles:
    active:
    - qa

management:
  endpoints:
    web:
      exposure:
        include: "*"
    
